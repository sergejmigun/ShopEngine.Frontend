app.registerModule('Lib', [function () {
        'use strict';
        return {
            module: {},
            initComponent: function (component) {
                return component;
            }
        };
    }]);
