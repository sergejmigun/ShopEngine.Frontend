app.registerModule('Services', [function () {
        'use strict';
        return {
            module: {},
            initComponent: function (component) {
                return component;
            }
        };
    }]);
