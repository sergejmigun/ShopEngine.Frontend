app.registerModule('Constants', [function () {
        'use strict';
        return {
            module: {},
            initComponent: function (component) {
                return component;
            }
        };
    }]);
