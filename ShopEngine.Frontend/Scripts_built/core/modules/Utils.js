app.registerModule('Utils', [function () {
        'use strict';
        return {
            module: {},
            initComponent: function (component) {
                return component;
            }
        };
    }]);
